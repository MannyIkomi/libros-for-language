const SHADOW = `0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)`;

const SHADOW_LG = `0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)`;
const SHADOW_2XL = `0 25px 50px -12px rgba(0, 0, 0, 0.25)`;

export const boxShadow = {
  boxShadow: SHADOW,
};
export const boxShadowLg = {
  boxShadow: SHADOW_LG,
};

export const boxShadow2xl = {
  boxShadow: SHADOW_2XL,
};
